/**
 * @module
 * `@baseless/core` — shared types, Zod schemas, and utilities used across all
 * Baseless modules.
 *
 * Includes:
 * - Authentication ceremony and component types
 * - Document, identity, session, and KV types
 * - Typed HTTP request/response wrappers
 * - Error classes and schema validation helpers
 * - ID generation (KSUID, base62), OTP, proof-of-work, and path matching
 */
